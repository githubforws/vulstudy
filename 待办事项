BUG:
1、新建的网卡如果退出时未主动销毁，那么docker network ls中会留下一个本地记录，但是网卡又不显示。
2、系统配置保存有时候会失效，刷新后恢复原样。
3、网卡创建成功后的提示ok显示样式应该为绿色勾
4、访问靶场的地址，如果有多个端口，应该给出对应端口的访问地址
5、场景的发布删除按钮有bug，不能正常显示

测试：
自动分配端口是否有端口重复校验？
三、代码审计 / 静态漏洞扫描（项目源码扫描，你的 Vulfocus 前端后端可用）
Semgrep：开源静态代码扫描，Java/Python/JS/Go，检测命令注入、SQL 拼接、不安全函数
SonarQube：代码规范 + 安全漏洞，支持 CI 流水线集成
Bandit：Python 专用安全扫描
ESLint + security 插件：前端 Vue/JS 漏洞检测

新功能：
1、添加一个/home网页
        需要修改的文件 ：

        文件 修改内容 影响程度 router/index.js 添加新路由 /home 极小（添加约10行代码） 新建 views/home/index.vue 创建首页组件 新增（不修改现有文件） styles/index.scss 添加必要的全局样式 极小

        路由配置示例 ：

        优点 ：

        1. ✅ 不修改任何现有代码 - 完全独立
        2. ✅ 完整保留参考模板效果 - 全屏 Hero、渐变背景等
        3. ✅ 通过路由切换导航 - 用户可自由切换
        4. ✅ 最小化风险 - 不影响现有功能
        注意事项 ：

        - 需要处理 Bootstrap 样式与 Element UI 的潜在冲突
        - 建议使用 scoped 样式 隔离
        - 可将参考模板的 CSS 转换为 SCSS 变量
        ## 是否需要大改？
        不需要大改 ，原因如下：

        1. 路由系统支持 ：Vue Router 天然支持多入口页面，不需要修改核心逻辑
        2. 布局独立 ：新页面不使用 Layout，避免侧边栏/导航栏干扰
        3. 样式隔离 ：通过 scoped 样式和 CSS Modules 避免冲突
        4. 资源复用 ：可以复用现有的 API 调用、工具函数等
        ## 总结
        问题 答案 是否可行？ ✅ 完全可行 需要大改吗？ ❌ 不需要，仅需新增文件和修改路由配置 会影响现有功能吗？ ❌ 不会，完全独立

        实现步骤 ：

        1. 创建 /home 路由页面组件
        2. 在路由中注册新路由
        3. 更新导航链接指向新首页（可选）
        如果你需要，我可以帮你实现这个新首页。
2、环境编排中的网卡拓展为可以接入至少四个容器
3、数据库迁移为mysql
        ## 📋 迁移方案总览
        ### 前置条件
        - ✅ PyMySQL==1.1.1 已在 requirements.txt 中
        - ✅ Django 版本为 4.2.24（支持 MySQL 8.0+）
        - ✅ 当前使用 SQLite3，数据库文件为 db.sqlite3
        ## 📝 详细迁移流程
        ### 第一步：准备 MySQL 环境
        ### 第二步：修改 Django 配置
        修改 settings.py ：

        ### 第三步：初始化 MySQL 数据库（迁移方式二选一） 方案A：使用 Django 迁移（推荐，适用于新部署） 方案B：数据迁移（适用于已有生产数据）
        ### 第四步：验证迁移结果
        ## ⚠️ 可能遇到的问题及解决方案
        ### 1. MySQL 客户端驱动问题
        问题 原因 解决方案 ImportError: No module named 'MySQLdb' Django 默认使用 MySQLdb，但安装了 PyMySQL 在 vulfocus/__init__.py 添加：
        import pymysql
        pymysql.install_as_MySQLdb() django.core.exceptions.ImproperlyConfigured MySQL 版本不兼容 使用 MySQL 8.0+，确保 PyMySQL 版本 >= 1.0

        ### 2. 字符集编码问题
        问题 原因 解决方案 Incorrect string value: '\xF0\x9F\x98\x8E' 数据库字符集不支持 emoji 数据库和连接都使用 utf8mb4 django.db.utils.OperationalError: (1273, ...) SQL 模式不兼容 添加 init_command: SET sql_mode='STRICT_TRANS_TABLES'

        ### 3. 字段类型兼容性问题
        问题 SQLite MySQL 解决方案 AutoField 溢出 SQLite 无限制 MySQL INT 最大约 21 亿 已使用 BigAutoField ，无需修改 TextField 索引 SQLite 允许 MySQL 5.6 前不允许 确保 MySQL >= 5.7 或移除 TextField 索引 unique=True 约束 SQLite 允许空值唯一 MySQL 空值不唯一 在模型中添加 default=""

        ### 4. 数据迁移问题
        问题 原因 解决方案 IntegrityError: UNIQUE constraint failed SQLite 和 MySQL 的唯一约束处理不同 先清理重复数据再迁移 django.db.utils.DataError: (1406, ...) 字段长度不够 检查 CharField 的 max_length loaddata 失败 外键约束问题 使用 --exclude=contenttypes 和 --exclude=auth.permission

        ### 5. Docker 部署问题
        如果使用 Docker 部署，需要：

        ## 📌 关键注意事项
        事项 说明 备份数据 迁移前务必备份 SQLite 数据库文件 事务测试 MySQL 的事务行为与 SQLite 不同，需测试事务相关功能 时区设置 MySQL 和 SQLite 的时区处理可能不同，需确保一致 连接池 生产环境建议使用连接池（如 django-db-connection-pool ） 性能调优 MySQL 需要配置合适的 innodb_buffer_pool_size 等参数

        ## 📊 迁移后验证清单
        - Django 服务正常启动
        - 登录功能正常
        - 镜像管理功能正常
        - 容器启动/停止功能正常
        - Flag 验证功能正常
        - 用户注册功能正常
        - 后台管理页面正常
4、flag注入功能
        ## 功能实现分析
        ### ✅ 存在的功能（环境编排模块）
        在 layout_image/views.py 中， 环境编排（Layout）模块 确实实现了将 flag 写入容器内部 /tmp/ 目录的功能：

        实现方式 ：通过 Docker API 的 exec_run() 在容器内执行 touch /tmp/flag-{bmhxxx} 命令，创建一个包含 flag 名称的空文件。

        ### ❌ 缺失的功能（普通容器和 Docker Compose）
        启动方式 文件 是否写入容器 问题 普通容器启动 container_tasks.py ❌ 否 仅保存到数据库 Docker Compose启动 compose_tasks.py ❌ 否 仅保存到数据库

        普通容器启动代码 （ container_tasks.py:137-138 ）：

        仅生成 flag 并保存到数据库 container_flag 字段， 没有 写入容器内部。

        ## 结论
        问题 答案 README 描述的功能是否存在？ ⚠️ 部分存在 ，仅环境编排模块实现了 普通容器启动是否有此功能？ ❌ 不存在 ，这是一个功能缺失 Docker Compose启动是否有此功能？ ❌ 不存在 ，同样缺失

        ## 问题影响
        由于普通容器和 Docker Compose 容器启动时 没有 将 flag 写入容器内部，导致：

        1. 用户无法通过攻破靶场获取 flag - 用户即使成功渗透靶场，也找不到 /tmp/ 下的 flag 文件
        2. flag 验证机制失效 - 当前系统的 flag 验证完全依赖用户手动输入后端生成的随机字符串，与靶场内容无关
        3. 不符合 CTF 靶场标准流程 - 正常的 CTF 靶场应该是用户攻破靶场后获取 flag，再提交验证
        如果你需要，我可以帮你修复这个问题，在普通容器和 Docker Compose 启动时添加将 flag 写入容器内部 /tmp/ 目录的功能。
        ## 将 Flag 注入靶场容器的方案
        ### 方案一：Docker API 写入文件（推荐）
        原理 ：容器启动后，通过 Docker API 向容器内部写入文件

        步骤 ：

        1. 生成 Flag（已有逻辑）
        2. 使用 client.containers.get(container_id).put_archive() 将 Flag 写入容器
        3. 在容器内创建 /flag.txt 文件，内容为 Flag
        优点 ：

        - 通用性强，适用于大多数镜像
        - 不需要修改镜像本身
        - Flag 存储在文件中，符合 CTF 习惯
        缺点 ：

        - 需要容器启动后才能写入
        - 需要处理文件权限问题
        操作示例 ：

        ### 方案二：环境变量注入
        原理 ：通过 Docker API 在运行容器时设置环境变量

        步骤 ：

        1. 生成 Flag
        2. 在 client.containers.run() 时添加 environment 参数
        3. 设置环境变量如 FLAG=xxxxxx
        优点 ：

        - 实现简单，不需要额外操作
        - 容器启动时即存在
        缺点 ：

        - 用户可能通过 env 命令直接看到
        - 某些镜像可能会打印环境变量到日志
        - 不符合 CTF 常规做法（Flag 应该在文件中）
        操作示例 ：

        ### 方案三：Volume 挂载（推荐）
        原理 ：在宿主机创建 Flag 文件，通过 Volume 挂载到容器内部

        步骤 ：

        1. 在宿主机的临时目录创建 /tmp/vulfocus/flag-{container_id}.txt
        2. 将 Flag 写入该文件
        3. 在运行容器时使用 -v 参数挂载到容器的 /flag.txt
        优点 ：

        - Flag 独立于容器，易于管理
        - 容器删除后文件仍可保留（可选）
        - 符合 CTF 习惯
        缺点 ：

        - 需要管理宿主机文件
        - 需要处理文件清理
        操作示例 ：

        ### 方案四：启动命令注入
        原理 ：通过修改容器启动命令，在容器启动时创建 Flag 文件

        步骤 ：

        1. 生成 Flag
        2. 修改容器启动命令，在原有命令前添加创建 Flag 的命令
        3. 使用 sh -c 组合命令
        优点 ：

        - Flag 在容器启动时创建
        - 不需要额外的 API 调用
        缺点 ：

        - 需要知道容器的原有启动命令
        - 某些镜像可能没有 sh/bash
        - 命令组合可能复杂
        操作示例 ：

        ### 方案五：自定义 Entrypoint 脚本
        原理 ：挂载自定义的 entrypoint 脚本，在脚本中创建 Flag 文件

        步骤 ：

        1. 创建通用的 entrypoint 脚本
        2. 脚本内容：接收 FLAG 环境变量，写入文件，然后执行原有命令
        3. 通过 Volume 挂载到容器，覆盖原有 entrypoint
        优点 ：

        - 标准化方案，适用于所有镜像
        - Flag 通过环境变量传入，脚本负责写入文件
        缺点 ：

        - 需要创建和管理脚本文件
        - 某些镜像的 entrypoint 可能复杂
        脚本示例 （ entrypoint.sh ）：

        操作示例 ：

        ### 方案对比
        方案 优点 缺点 推荐度 API写入文件 通用，无需修改镜像 容器启动后写入，权限问题 ⭐⭐⭐⭐ 环境变量 简单，启动时即存在 可能被直接查看 ⭐⭐ Volume挂载 文件独立，易于管理 需要管理宿主机文件 ⭐⭐⭐⭐⭐ 启动命令 启动时创建，无需额外API 需要知道原有命令 ⭐⭐⭐ 自定义Entrypoint 标准化，通用 需要管理脚本文件 ⭐⭐⭐⭐

        ### 推荐方案
        首选：Volume 挂载 + 环境变量注入

        结合方案二和方案三：

        1. 通过环境变量传入 Flag
        2. 通过 Volume 挂载自定义 entrypoint 脚本
        3. 脚本将环境变量写入 /flag.txt
        优势 ：

        - 不需要知道容器原有命令
        - 适用于所有镜像
        - Flag 存储在文件中，符合 CTF 习惯
        - 易于管理和清理
        流程 ：

        ```
        生成 Flag
            ↓
        设置环境变量 FLAG=xxx
            ↓
        挂载 entrypoint.sh
            ↓
        容器启动 → entrypoint.sh 执行 → 创建 /flag.txt
            ↓
        用户攻破靶场 → 读取 /flag.txt
            ↓
        用户提交 Flag → 系统验证
        ```